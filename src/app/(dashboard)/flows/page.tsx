/*
File description:
This Flows page is intentionally blank while the automation reporting experience is redesigned. The route
stays mounted inside the authenticated dashboard shell so the future UI can be rebuilt without changing
access control or navigation contracts.
*/

export default function FlowsPage() {
  // Keep this route empty until the new flow page design is implemented.
  return null;
}
