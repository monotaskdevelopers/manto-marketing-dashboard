/*
File description:
This Shopify page is intentionally blank while the ecommerce reporting experience is redesigned. The
route keeps its protected dashboard shell but avoids loading report data until the new UI is built.
*/

export default function ShopifyPage() {
  // Keep this route empty until the new Shopify page design is implemented.
  return null;
}
