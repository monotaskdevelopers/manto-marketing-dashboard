/*
File description:
This Dashboard page is the intentionally blank primary workspace route. It keeps the authenticated app
shell and navigation available while the reporting UI is redesigned from the ground up.
*/

export default function DashboardPage() {
  // The page body is intentionally empty while the new dashboard experience is designed.
  return null;
}
